# FinanceAI — Python dependencies
# Install with:  pip install -r requirements.txt

# Anthropic Claude API
anthropic>=0.25.0

# PDF parsing (pdfplumber is preferred; PyPDF2 is the fallback)
pdfplumber>=0.10.0
PyPDF2>=3.0.0             # fallback if pdfplumber unavailable

# Excel spreadsheet support
openpyxl>=3.1.0

# Standard library covers csv, json, logging, argparse — no extra packages needed.
